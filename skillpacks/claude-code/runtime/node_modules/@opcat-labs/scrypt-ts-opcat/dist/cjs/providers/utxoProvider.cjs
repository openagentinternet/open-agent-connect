"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getUtxoKey = exports.addChangeUtxoToProvider = exports.markSpent = void 0;
const common_js_1 = require('../utils/common.cjs');
/**
 * Marks all inputs of a transaction as spent in the UTXO provider.
 * @param utxoProvider - The UTXO provider to update.
 * @param tx - The transaction whose inputs should be marked as spent.
 */
function markSpent(utxoProvider, tx) {
    for (let i = 0; i < tx.inputs.length; i++) {
        const input = tx.inputs[i];
        utxoProvider.markSpent((0, common_js_1.getTxId)(input), input.outputIndex);
    }
}
exports.markSpent = markSpent;
/** @ignore */
function addChangeUtxoToProvider(utxoProvider, psbt) {
    const changeUtxo = psbt.getChangeUTXO();
    if (changeUtxo) {
        utxoProvider.addNewUTXO(changeUtxo);
    }
}
exports.addChangeUtxoToProvider = addChangeUtxoToProvider;
/** @ignore */
function getUtxoKey(utxo) {
    return `${utxo.txId}:${utxo.outputIndex}`;
}
exports.getUtxoKey = getUtxoKey;
//# sourceMappingURL=utxoProvider.js.map