"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.buildStateOutputImpl = exports.buildChangeOutputImpl = void 0;
const assert_js_1 = require('../fns/assert.cjs');
const byteString_js_1 = require('../fns/byteString.cjs');
const txUtils_js_1 = require('../builtin-libs/txUtils.cjs');
const hashes_js_1 = require('../fns/hashes.cjs');
/**
 * @ignore
 * Builds a change output for a PSBT (Partially Signed Bitcoin Transaction).
 *
 * @param psbt - The contextual PSBT containing change information.
 * @returns The serialized change output as a ByteString. Returns empty ByteString if no change script exists.
 */
function buildChangeOutputImpl(psbt) {
    const changeInfo = psbt.getChangeInfo();
    if (changeInfo.satoshis === 0n) {
        return (0, byteString_js_1.toByteString)('');
    }
    // Build P2PKH script from pubkeyhash, then hash to get scriptHash
    const p2pkhScript = txUtils_js_1.TxUtils.buildP2PKHScript(changeInfo.pubkeyhash);
    return txUtils_js_1.TxUtils.buildDataOutput((0, hashes_js_1.sha256)(p2pkhScript), changeInfo.satoshis, changeInfo.dataHash);
}
exports.buildChangeOutputImpl = buildChangeOutputImpl;
/**
 * @ignore
 * Builds a state output for an OP_CAT enabled smart contract.
 *
 * @param self - The contract instance
 * @param state - The contract state to serialize
 * @param satoshis - The amount in satoshis for the output
 * @param script - The locking script for the output
 * @returns The built output as ByteString
 * @throws Throws if script is empty
 */
function buildStateOutputImpl(self, state, satoshis, scriptHash) {
    const lenScriptHash = (0, byteString_js_1.len)(scriptHash);
    (0, assert_js_1.assert)(lenScriptHash === 32n, 'invalid ScriptHash');
    const Class = Object.getPrototypeOf(self).constructor;
    return txUtils_js_1.TxUtils.buildDataOutput(scriptHash, satoshis, (0, hashes_js_1.sha256)(Class.serializeState(state)));
}
exports.buildStateOutputImpl = buildStateOutputImpl;
//# sourceMappingURL=buildOutput.js.map