"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.StateLib = void 0;
/* eslint-disable @typescript-eslint/no-explicit-any */
const smartContractLib_js_1 = require('../smartContractLib.cjs');
const abi_js_1 = require('../abi.cjs');
const stateSerializer_js_1 = require('../stateSerializer.cjs');
const abiutils_js_1 = require('../abiutils.cjs');
const index_js_1 = require('../fns/index.cjs');
/**
 * Library for computing the hash of a state.
 * @category Library
 * @onchain
 */
class StateLib extends smartContractLib_js_1.SmartContractLib {
    /**
     * Serializes the given state object into a ByteString using the library's artifact.
     * @template T - Type of the state object extending OpcatState
     * @param this - Reference to the StateLib class constructor
     * @param state - State object to be serialized
     * @returns Serialized state as ByteString
     * @throws Error if artifact is not loaded, library not found in artifact, or state type is undefined
     * @category State
     */
    static serializeState(state) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const libraryClazz = this;
        const artifact = libraryClazz.artifact;
        if (!artifact) {
            throw new Error(`Artifact is not loaded for the library: ${this.name}`);
        }
        const abiCoder = new abi_js_1.ABICoder(artifact);
        const library = abiCoder.artifact.library.find((lib) => (0, abiutils_js_1.getUnRenamedSymbol)(lib.name) === (0, abiutils_js_1.getUnRenamedSymbol)(this.name));
        if (!library) {
            throw new Error(`Library ${this.name} is not found in the artifact`);
        }
        if (!library.stateType) {
            throw new Error(`State type is not defined for the library: ${this.name}`);
        }
        return (0, stateSerializer_js_1.serializeState)(artifact, libraryClazz.stateType, state);
    }
    /**
     * Deserializes a state object from its ByteString representation.
     *
     * @template T - Type of the state object extending OpcatState
     * @param this - Reference to the StateLib class constructor
     * @param serializedState - ByteString containing the serialized state data
     * @returns The deserialized state object of type T
     * @throws Error if artifact is not loaded, library is not found, or state type is undefined
     */
    static deserializeState(serializedState) {
        // eslint-disable-next-line @typescript-eslint/no-explicit-any
        const libraryClazz = this;
        const artifact = libraryClazz.artifact;
        if (!artifact) {
            throw new Error(`Artifact is not loaded for the library: ${this.name}`);
        }
        const abiCoder = new abi_js_1.ABICoder(artifact);
        const library = abiCoder.artifact.library.find((lib) => (0, abiutils_js_1.getUnRenamedSymbol)(lib.name) === (0, abiutils_js_1.getUnRenamedSymbol)(this.name));
        if (!library) {
            throw new Error(`Library ${this.name} is not found in the artifact`);
        }
        if (!library.stateType) {
            throw new Error(`State type is not defined for the library: ${this.name}`);
        }
        return (0, stateSerializer_js_1.deserializeState)(artifact, libraryClazz.stateType, serializedState);
    }
    /**
     * Computes the SHA-256 hash of a serialized state object.
     * @template T - Type extending OpcatState
     * @param state - The state object to hash
     * @returns The hash as a ByteString
     */
    static stateHash(state) {
        return (0, index_js_1.sha256)(this.serializeState(state));
    }
}
exports.StateLib = StateLib;
//# sourceMappingURL=stateLib.js.map