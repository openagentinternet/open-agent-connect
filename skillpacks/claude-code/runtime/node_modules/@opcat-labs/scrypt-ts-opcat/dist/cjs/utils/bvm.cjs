"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.bvmVerify = void 0;
const opcat_1 = require("@opcat-labs/opcat");
/**
 * Verify that an input of `ExtPsbt` can be unlocked correctly. The extPsbt should be finalized.
 * @param extPsbt
 * @param inputIndex
 * @returns true if success
 */
function bvmVerify(extPsbt, inputIndex = 0) {
    if (!extPsbt.isFinalized) {
        throw new Error('ExtPsbt is not finalized');
    }
    const prevOuts = extPsbt.unsignedTx.inputs.map((input) => input.output);
    opcat_1.Interpreter.MAX_SCRIPT_ELEMENT_SIZE = Number.MAX_SAFE_INTEGER;
    opcat_1.Interpreter.MAXIMUM_ELEMENT_SIZE = Number.MAX_SAFE_INTEGER;
    const interp = new opcat_1.Interpreter();
    const prevScript = prevOuts[inputIndex].script;
    const prevSatoshi = Number(prevOuts[inputIndex].satoshis);
    const finalScriptSig = extPsbt.data.inputs[inputIndex].finalScriptSig || Uint8Array.from([]);
    const flags = opcat_1.Interpreter.DEFAULT_FLAGS;
    const ret = interp.verify(opcat_1.Script.fromBuffer(Buffer.from(finalScriptSig)), prevScript, extPsbt.unsignedTx, inputIndex, flags, prevSatoshi);
    if (!ret) {
        return interp.errstr;
    }
    return true;
}
exports.bvmVerify = bvmVerify;
//# sourceMappingURL=bvm.js.map