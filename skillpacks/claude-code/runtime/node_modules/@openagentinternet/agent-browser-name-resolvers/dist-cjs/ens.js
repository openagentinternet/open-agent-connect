"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.createEnsOpenAgentInternetResolver = createEnsOpenAgentInternetResolver;
const agent_browser_core_1 = require("@openagentinternet/agent-browser-core");
const viem_1 = require("viem");
const chains_1 = require("viem/chains");
const ens_1 = require("viem/ens");
function normalizeRpcUrls(value) {
    return value.map((item) => item.trim()).filter(Boolean);
}
function createDefaultClient(rpcUrl) {
    return (0, viem_1.createPublicClient)({
        chain: chains_1.mainnet,
        transport: (0, viem_1.http)(rpcUrl),
    });
}
function createEnsOpenAgentInternetResolver(input) {
    const rpcUrls = normalizeRpcUrls(input.rpcUrls);
    const textKey = input.textKey?.trim() || agent_browser_core_1.OPEN_AGENT_INTERNET_ENS_TEXT_KEY;
    const now = input.now ?? Date.now;
    const transportFactory = input.transportFactory ?? createDefaultClient;
    return {
        id: 'ens',
        supportsName(name) {
            return (0, agent_browser_core_1.isSupportedNameAliasId)(name);
        },
        async resolveNameAlias(request) {
            let normalizedName;
            try {
                normalizedName = (0, ens_1.normalize)(request.name);
            }
            catch (error) {
                return (0, agent_browser_core_1.browserCommandFailed)('name_resolution_failed', error instanceof Error ? error.message : 'ENS name normalization failed.', {
                    aliasName: request.name,
                });
            }
            if (rpcUrls.length === 0) {
                return (0, agent_browser_core_1.browserCommandFailed)('name_resolution_unavailable', 'ENS RPC URLs are not configured.', {
                    aliasName: normalizedName,
                    textKey,
                });
            }
            const errors = [];
            for (const rpcUrl of rpcUrls) {
                try {
                    const client = transportFactory(rpcUrl);
                    const textValue = await client.getEnsText({ name: normalizedName, key: textKey });
                    const canonicalUri = typeof textValue === 'string' ? textValue.trim() : '';
                    if (canonicalUri) {
                        return (0, agent_browser_core_1.browserCommandSuccess)({
                            provider: 'ens',
                            normalizedName,
                            textKey,
                            canonicalUri,
                            resolvedAt: now(),
                            verificationState: 'partial',
                            raw: { rpcUrl },
                        });
                    }
                }
                catch (error) {
                    errors.push(error instanceof Error ? error.message : String(error));
                }
            }
            if (errors.length === rpcUrls.length) {
                return (0, agent_browser_core_1.browserCommandFailed)('name_resolution_failed', 'ENS text record lookup failed.', {
                    aliasName: normalizedName,
                    textKey,
                    errors,
                });
            }
            return (0, agent_browser_core_1.browserCommandFailed)('name_alias_not_found', 'ENS text record was missing or empty.', {
                aliasName: normalizedName,
                textKey,
            });
        },
    };
}
//# sourceMappingURL=ens.js.map