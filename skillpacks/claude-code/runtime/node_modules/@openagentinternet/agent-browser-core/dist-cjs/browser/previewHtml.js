"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.rewritePreviewHtmlMetafileReferences = rewritePreviewHtmlMetafileReferences;
exports.preparePreviewHtml = preparePreviewHtml;
const metafileContentUrl_js_1 = require("./metafileContentUrl.js");
const metafileResolver_js_1 = require("./metafileResolver.js");
// Preview HTML is served by the host, so MetaApps can rely on Agent Internet
// URIs without embedding host-specific helpers themselves:
// - subresource attributes (src/srcset/poster) holding metafile:// references
//   are rewritten to accelerated web2 content URLs before serving;
// - <a href="metaid://|metaapp://|metafile://|map://|pin://"> links would be
//   silently dropped by the sandboxed preview iframe, so a navigation bridge
//   script is injected to forward them to the Browser via postMessage;
// - localStorage/sessionStorage get a memory fallback because the preview
//   iframe runs in an opaque origin.
const METAFILE_URI_RE = /^metafile:\/\/(.*)$/iu;
// The captured prefix (start-of-string or whitespace) keeps the match anchored
// to real attribute names, so data-src="metafile://..." stays untouched.
const SUBRESOURCE_ATTRIBUTE_RE = /(^|\s)(src|srcset|poster)\s*=\s*(?:"([^"]*)"|'([^']*)')/giu;
const PREVIEW_STORAGE_SHIM_MARKER = '__agentBrowserPreviewStorageShim';
const PREVIEW_NAVIGATION_BRIDGE_MARKER = '__agentBrowserPreviewBridge';
const PREVIEW_STORAGE_SHIM = `<script>
(function __agentBrowserPreviewStorageShim() {
  function storageIsAvailable(name) {
    try {
      var storage = window[name];
      var key = '__agent_browser_preview_storage_probe__';
      storage.setItem(key, key);
      storage.removeItem(key);
      return true;
    } catch (_) {
      return false;
    }
  }

  function createMemoryStorage() {
    var values = Object.create(null);
    return {
      get length() {
        return Object.keys(values).length;
      },
      key: function key(index) {
        var keys = Object.keys(values);
        return keys[index] || null;
      },
      getItem: function getItem(key) {
        key = String(key);
        return Object.prototype.hasOwnProperty.call(values, key) ? values[key] : null;
      },
      setItem: function setItem(key, value) {
        values[String(key)] = String(value);
      },
      removeItem: function removeItem(key) {
        delete values[String(key)];
      },
      clear: function clear() {
        values = Object.create(null);
      }
    };
  }

  function installFallback(name) {
    if (storageIsAvailable(name)) return;
    try {
      Object.defineProperty(window, name, {
        value: createMemoryStorage(),
        configurable: true
      });
    } catch (_) {}
  }

  installFallback('localStorage');
  installFallback('sessionStorage');
}());
</script>`;
const PREVIEW_NAVIGATION_BRIDGE = `<script>
(function __agentBrowserPreviewBridge() {
  var INTERNAL_HREF_RE = /^(metaid|metaapp|metafile|map|pin):\\/\\//i;
  var bridge = window.AgentBrowser || {};
  bridge.navigate = bridge.navigate || function (uri) {
    window.parent.postMessage({
      type: 'agent-browser:navigate',
      version: 1,
      uri: String(uri || '')
    }, '*');
  };
  window.AgentBrowser = bridge;
  // Capture phase + stopPropagation: exactly one navigation per internal-URI
  // link click, even when the app ships its own link interceptor.
  document.addEventListener('click', function (event) {
    var target = event.target;
    var link = target && target.closest ? target.closest('a[href]') : null;
    if (!link) return;
    var href = link.getAttribute('href') || '';
    if (!INTERNAL_HREF_RE.test(href)) return;
    event.preventDefault();
    event.stopPropagation();
    event.stopImmediatePropagation();
    window.AgentBrowser.navigate(href);
  }, true);
}());
</script>`;
function escapeHtmlAttribute(value, quote) {
    return value
        .replace(/&/gu, '&amp;')
        .replace(quote === '"' ? /"/gu : /'/gu, quote === '"' ? '&quot;' : '&#39;');
}
function rewriteSingleReference(value, metafileContentBaseUrl) {
    const uriMatch = value.match(METAFILE_URI_RE);
    if (!uriMatch) {
        return value;
    }
    const pinId = (0, metafileResolver_js_1.normalizeMetafilePinId)(uriMatch[1]);
    return pinId ? (0, metafileContentUrl_js_1.buildMetafileAcceleratedContentUrl)(metafileContentBaseUrl, pinId) : value;
}
function rewriteSrcsetReference(value, metafileContentBaseUrl) {
    return value.split(',').map((candidate) => {
        const trimmed = candidate.trim();
        const parts = trimmed.split(/\s+/u);
        if (!parts[0]) {
            return trimmed;
        }
        const rewritten = rewriteSingleReference(parts[0], metafileContentBaseUrl);
        return rewritten === parts[0]
            ? trimmed
            : [rewritten, ...parts.slice(1)].join(' ');
    }).join(', ');
}
function rewritePreviewHtmlMetafileReferences(html, metafileContentBaseUrl) {
    return html.replace(SUBRESOURCE_ATTRIBUTE_RE, (match, prefix, attribute, doubleQuoted, singleQuoted) => {
        const quote = doubleQuoted !== undefined ? '"' : "'";
        const value = doubleQuoted !== undefined ? doubleQuoted : (singleQuoted ?? '');
        const rewritten = attribute.toLowerCase() === 'srcset'
            ? rewriteSrcsetReference(value, metafileContentBaseUrl)
            : rewriteSingleReference(value, metafileContentBaseUrl);
        return rewritten === value
            ? match
            : `${prefix}${attribute}=${quote}${escapeHtmlAttribute(rewritten, quote)}${quote}`;
    });
}
function injectHeadScript(html, script) {
    const headMatch = html.match(/<head\b[^>]*>/iu);
    if (headMatch?.index !== undefined) {
        const insertAt = headMatch.index + headMatch[0].length;
        return `${html.slice(0, insertAt)}${script}${html.slice(insertAt)}`;
    }
    const firstScriptIndex = html.search(/<script\b/iu);
    if (firstScriptIndex >= 0) {
        return `${html.slice(0, firstScriptIndex)}${script}${html.slice(firstScriptIndex)}`;
    }
    return `${script}${html}`;
}
function preparePreviewHtml(input) {
    if (!/^text\/html\b/iu.test(input.contentType)) {
        return input.body;
    }
    let html = rewritePreviewHtmlMetafileReferences(input.body.toString('utf8'), input.metafileContentBaseUrl);
    if (!html.includes(PREVIEW_STORAGE_SHIM_MARKER)) {
        html = injectHeadScript(html, PREVIEW_STORAGE_SHIM);
    }
    if (!html.includes(PREVIEW_NAVIGATION_BRIDGE_MARKER)) {
        html = injectHeadScript(html, PREVIEW_NAVIGATION_BRIDGE);
    }
    return html;
}
//# sourceMappingURL=previewHtml.js.map