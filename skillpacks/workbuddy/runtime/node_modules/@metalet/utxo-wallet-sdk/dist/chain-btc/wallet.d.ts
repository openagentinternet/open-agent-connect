import { SignTypeMap, SignParams } from './types';
import { Net, CoinType, SignType, BaseWallet, BaseHotWallet, AddressType, Change } from '../chain-base';

export declare class BtcWallet extends BaseWallet {
    constructor({ seed, network, mnemonic, coinType, addressType, addressIndex, change, }: {
        network: Net;
        seed?: Buffer;
        change?: Change;
        mnemonic: string;
        addressIndex: number;
        addressType: AddressType;
        coinType: CoinType.MVC | CoinType.BSV | number;
    });
    signTx<T extends SignType>(signType: T, signParams: SignParams[T]): SignTypeMap[T];
}
export declare class BtcHotWallet extends BaseHotWallet {
    constructor({ network, publicKey, addressType, }: {
        network: Net;
        publicKey: string;
        addressType: AddressType;
    });
    buildTx<T extends SignType>(signType: T, signParams: SignParams[T]): SignTypeMap[T];
}
