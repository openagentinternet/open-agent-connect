import { ScriptType, UTXO } from '../../chain-base';
import { BtcWallet, BtcHotWallet } from '../wallet';
import { default as TransactionTool } from '../wallet-tools/TransactionTool';

interface RuneData {
    address: string;
    amount: string;
}
interface StakeData {
    utxos: UTXO[];
    amount: string;
    address: string;
    publicKey: string;
    scriptType: ScriptType;
}
export interface RuneStakeParams {
    utxos: UTXO[];
    runeId: string;
    feeRate: number;
    publicKey?: string;
    runeData: RuneData[];
    outputValue?: number;
    stakeData: StakeData[];
    changeAddress?: string;
    scriptType?: ScriptType;
}
export declare class RuneStakeTool<T extends BtcWallet | BtcHotWallet> extends TransactionTool<T> {
    stake({ utxos, runeId, feeRate, runeData, stakeData, publicKey, scriptType, outputValue, changeAddress, }: RuneStakeParams): import('..').TxDetail;
    private parseRuneId;
    private buildStakePsbt;
    private selectRuneUTXOs;
}
export {};
