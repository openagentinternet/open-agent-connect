import { ScriptType, UTXO } from '../../chain-base';
import { BtcWallet, BtcHotWallet } from '../wallet';
import { default as TransactionTool } from '../wallet-tools/TransactionTool';

interface RuneData {
    address: string;
    amount: string;
}
export interface RuneSwapParams {
    utxos: UTXO[];
    runeId: string;
    feeRate: number;
    runeUtxos: UTXO[];
    publicKey?: string;
    runeData: RuneData[];
    outputValue?: number;
    changeAddress?: string;
    scriptType?: ScriptType;
    outputs?: {
        address: string;
        satoshis: number;
    }[];
}
export declare class RuneSwapTool<T extends BtcWallet | BtcHotWallet> extends TransactionTool<T> {
    swap({ utxos, runeId, feeRate, outputs, runeData, publicKey, runeUtxos, scriptType, outputValue, changeAddress, }: RuneSwapParams): import('..').TxDetail;
    private parseRuneId;
    private buildSwapPsbt;
    private selectRuneUTXOs;
}
export {};
