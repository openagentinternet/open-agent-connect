import { default as Decimal } from 'decimal.js';
import { mvcCoinType } from '../service-mvc';
import { Net, BaseWallet, ScriptType, AddressType, BtcHotWallet, DogeWallet } from '@metalet/utxo-wallet-sdk';

export type { mvcCoinType, Net };
export { ScriptType };
export declare enum Chain {
    BTC = "btc",
    MVC = "mvc",
    DOGE = "doge"
}
export type Account = {
    name: string;
    addressIndex: number;
    chainWallets: {
        [Chain.BTC]?: BaseWallet[];
        [Chain.MVC]?: BaseWallet[];
        [Chain.DOGE]?: DogeWallet[];
    };
};
export type Wallet = {
    name: string;
    balance: Decimal;
    mnemonic: string;
    mvcTypes: mvcCoinType[];
    accounts: {
        [accountId: string]: Account;
    };
};
export interface Manager {
    [walletId: string]: Wallet;
}
export interface WalletOptions {
    id?: string;
    name?: string;
    seed?: Buffer;
    mnemonic: string;
    mvcTypes?: mvcCoinType[];
    accountsOptions: AccountOptions[];
}
export interface AccountOptions {
    id?: string;
    name?: string;
    addressIndex: number;
}
export interface HotManager {
    [walletId: string]: {
        name: string;
        wallet: BtcHotWallet;
    };
}
export interface HotWalletOptions {
    id?: string;
    chain: Chain;
    name?: string;
    publicKey: string;
    addressType: AddressType;
}
