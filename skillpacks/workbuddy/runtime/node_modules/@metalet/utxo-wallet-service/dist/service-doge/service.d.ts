import { mvcCoinType } from '../service-mvc/types';
import { DogeWallet, Net } from '@metalet/utxo-wallet-sdk';

declare class DogeService {
    createAccount({ seed, network, mnemonic, addressIndex, mvcTypes, }: {
        network: Net;
        seed?: Buffer;
        mnemonic: string;
        addressIndex: number;
        mvcTypes?: mvcCoinType[];
    }): DogeWallet[];
}
export { DogeService };
