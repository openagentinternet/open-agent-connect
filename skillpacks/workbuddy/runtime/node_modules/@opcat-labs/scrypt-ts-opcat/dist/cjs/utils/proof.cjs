"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.getBackTraceInfo = exports.toTxHashPreimage = exports.toGenesisOutpoint = void 0;
const opcat_1 = require("@opcat-labs/opcat");
const consts_js_1 = require('../smart-contract/consts.cjs');
const index_js_1 = require('../smart-contract/fns/index.cjs');
const stdUtils_js_1 = require('../smart-contract/builtin-libs/stdUtils.cjs');
const bufferutils_js_1 = require('../psbt/bufferutils.cjs');
const uint8array_tools_1 = require("uint8array-tools");
const common_js_1 = require('./common.cjs');
/**
 * Converts a UTXO to its genesis outpoint format.
 * The genesis outpoint is constructed by reversing the txId bytes and appending
 * the output index as a 4-byte little-endian string.
 *
 * @param utxo - The UTXO to convert
 * @returns The genesis outpoint as a hex-encoded ByteString
 */
const toGenesisOutpoint = function (utxo) {
    return (0, common_js_1.uint8ArrayToHex)((0, common_js_1.hexToUint8Array)(utxo.txId).reverse()) + (0, index_js_1.intToByteString)(BigInt(utxo.outputIndex), 4n);
};
exports.toGenesisOutpoint = toGenesisOutpoint;
/**
 * convert raw txHash preimage buffer to TxHashPreimage struct
 * @param txHashPreimageBuf
 * @returns
 */
const toTxHashPreimage = function (txHashPreimageBuf) {
    const br = new bufferutils_js_1.BufferReader(txHashPreimageBuf);
    const version = (0, uint8array_tools_1.toHex)(br.readSlice(4n));
    const inputCountVal = br.readVarInt();
    let inputList = (0, index_js_1.toByteString)('');
    for (let i = 0; i < inputCountVal; i++) {
        inputList += (0, uint8array_tools_1.toHex)(br.readSlice(36n + 32n + 4n));
    }
    const outputCountVal = br.readVarInt();
    let outputList = (0, index_js_1.toByteString)('');
    for (let i = 0; i < outputCountVal; i++) {
        outputList += (0, uint8array_tools_1.toHex)(br.readSlice(8n + 32n + 32n));
    }
    const nLockTime = (0, uint8array_tools_1.toHex)(br.readSlice(4n));
    return {
        version,
        inputList,
        outputList,
        nLockTime,
    };
};
exports.toTxHashPreimage = toTxHashPreimage;
/**
 * prevPrevTx: input1 + input2 + ... = output1 + output2 + ...
 * prevTx: input1(prevPrevTx.output1) + input2 + ... = output1 + output2 + ...
 * curTx: input1(prevTx.output1) + input2 + ... = output1 + output2 + ...
 *
 * if we want to backtrace curTx.input1, the arguments should be:
 *
 * @param prevTxHex: prevTx.toHex()
 * @param prevPrevTxHex: prevPrevTx.toHex()
 * @param prevTxInputIndex: prevTx.input1.inputIndex, here is 0
 * @returns
 */
function getBackTraceInfo(prevTxHex, prevPrevTxHex, prevTxInputIndex) {
    const prevTx = new opcat_1.Transaction(prevTxHex);
    const prevPrevTx = new opcat_1.Transaction(prevPrevTxHex);
    const prevTxPreimage = (0, exports.toTxHashPreimage)(prevTx.toTxHashPreimage());
    const prevPrevTxPreimage = (0, exports.toTxHashPreimage)(prevPrevTx.toTxHashPreimage());
    let index = 0n;
    const txInputBytes = (0, index_js_1.slice)(prevTxPreimage.inputList, BigInt(prevTxInputIndex) * consts_js_1.TX_INPUT_BYTE_LEN, (BigInt(prevTxInputIndex) + 1n) * consts_js_1.TX_INPUT_BYTE_LEN);
    const prevTxInput = {
        prevTxHash: (0, index_js_1.slice)(txInputBytes, index, (index += 32n)),
        prevOutputIndex: stdUtils_js_1.StdUtils.fromLEUnsigned((0, index_js_1.slice)(txInputBytes, index, (index += 4n))),
        scriptHash: (0, index_js_1.slice)(txInputBytes, index, (index += 32n)),
        sequence: stdUtils_js_1.StdUtils.fromLEUnsigned((0, index_js_1.slice)(txInputBytes, index, (index += 4n))),
    };
    return {
        prevTxInput,
        prevTxInputIndex: BigInt(prevTxInputIndex),
        prevPrevTxPreimage,
    };
}
exports.getBackTraceInfo = getBackTraceInfo;
;
//# sourceMappingURL=proof.js.map