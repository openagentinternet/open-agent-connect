import { SendParams } from '../types';
import { default as TransactionTool } from './TransactionTool';
import { BtcWallet, BtcHotWallet } from '../wallet';

export default class SendWalletTool<T extends BtcWallet | BtcHotWallet> extends TransactionTool<T> {
    constructor(wallet: T, hotMode?: boolean);
    private buildPsbt;
    send({ utxos, feeRate, changeAddress, outputs }: SendParams): import('..').TxDetail;
}
