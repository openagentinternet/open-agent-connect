import { type BrowserPageDefinition } from './app.js';
import { type BrowserTheme } from './theme.js';
export interface RenderBrowserPageHtmlOptions {
    /**
     * Initial Browser theme. Defaults to `light` to preserve the legacy
     * behavior when no theme is supplied. The resolved theme is baked into the
     * HTML so the first paint is already correct (no white flash).
     */
    theme?: BrowserTheme;
}
export declare function renderBrowserPageHtml(definition?: BrowserPageDefinition, languagePreference?: string | null, options?: RenderBrowserPageHtmlOptions): Promise<string>;
//# sourceMappingURL=page.d.ts.map