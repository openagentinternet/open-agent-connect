import { BtcWallet } from '../wallet';
import { default as TaprootTool } from '../wallet-tools/TaprootTool';

interface TxOut {
    value: number;
    address: string;
}
export type InscriptionData = {
    contentType: string;
    body: string | Buffer;
    revealAddr: string;
};
export interface TxCtxData {
    hash: Buffer;
    witness: Buffer[];
    revealAddress: string;
    inscriptionScript: Buffer;
    revealTxPrevOutput: TxOut;
    commitTxAddressPkScript: Buffer;
}
export declare class BRC20Tool extends TaprootTool {
    constructor(wallet: BtcWallet);
    private createScript;
    createInscriptionTxCtxData(publicKey: Buffer, inscriptionData: InscriptionData, revealOutValue?: number): TxCtxData;
}
export {};
