import { TxDetail } from './types';
import { UTXO } from '../chain-base';
import { BtcWallet, BtcHotWallet } from './wallet';
import { default as TransactionTool } from './wallet-tools/TransactionTool';

interface RuneData {
    address: string;
    amount: string;
}
export interface RuneMintParams {
    runeId: string;
    utxos: UTXO[];
    feeRate: number;
    mintNum: number;
    outputValue?: number;
}
export interface RuneSendParams {
    utxos: UTXO[];
    runeId: string;
    feeRate: number;
    runeUtxos: UTXO[];
    runeData: RuneData[];
    outputValue?: number;
}
export declare class RunesTool<T extends BtcWallet | BtcHotWallet> extends TransactionTool<T> {
    mint({ runeId, utxos, feeRate, mintNum, outputValue }: RuneMintParams): {
        baseTx: TxDetail;
        leftTxs: TxDetail[];
    };
    send({ utxos, runeId, feeRate, runeData, runeUtxos, outputValue, }: RuneSendParams): TxDetail;
    private parseRuneId;
    private buildDummyPsbt;
    private buildBaseMintPsbt;
    private buildLeftMintPsbt;
    private buildSendPsbt;
    private selectRuneUTXOs;
}
export {};
