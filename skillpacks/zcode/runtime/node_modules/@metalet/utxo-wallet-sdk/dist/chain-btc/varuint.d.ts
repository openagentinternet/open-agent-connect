import { Buffer } from 'buffer';

declare function encode(number: number, buffer?: Buffer, offset?: number): Buffer;
declare function decode(buffer: Buffer, offset?: number): number;
declare function encodingLength(number: number): number;
export { encode, decode, encodingLength };
