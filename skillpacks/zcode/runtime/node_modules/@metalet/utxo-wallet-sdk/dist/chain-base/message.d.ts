import { Network } from 'bitcoinjs-lib';

export declare function sign(wifPrivate: string, message: string, network?: Network, encoding?: BufferEncoding): string;
export declare function verify(publicKey: string, message: string, sig: string, encoding?: BufferEncoding): boolean;
