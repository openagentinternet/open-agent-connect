import { Net, CoinType, BaseWallet, AddressType } from '../chain-base';

export declare class MvcWallet extends BaseWallet {
    constructor({ seed, network, mnemonic, coinType, addressType, addressIndex, }: {
        seed?: Buffer;
        network: Net;
        mnemonic: string;
        addressIndex: number;
        addressType: AddressType;
        coinType: CoinType.MVC | CoinType.BSV | number;
    });
}
