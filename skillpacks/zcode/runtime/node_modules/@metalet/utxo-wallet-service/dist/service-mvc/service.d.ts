import { mvcCoinType } from './types';
import { BaseService } from '../service-base';
import { MvcWallet, Net } from '@metalet/utxo-wallet-sdk';

declare class MvcService extends BaseService {
    createAccount({ seed, network, mnemonic, addressIndex, mvcTypes, }: {
        network: Net;
        seed?: Buffer;
        mnemonic: string;
        addressIndex: number;
        mvcTypes?: mvcCoinType[];
    }): MvcWallet[];
}
export { MvcService };
