import { BaseService } from '../service-base';
import { mvcCoinType } from '../service-mvc/types';
import { BtcWallet, Net } from '@metalet/utxo-wallet-sdk';

declare class BtcService extends BaseService {
    createAccount({ seed, network, mnemonic, addressIndex, mvcTypes, }: {
        network: Net;
        seed?: Buffer;
        mnemonic: string;
        addressIndex: number;
        mvcTypes?: mvcCoinType[];
    }): BtcWallet[];
}
export { BtcService };
