import { TransferParams } from '../types';
import { default as TransactionTool } from './TransactionTool';
import { BtcWallet, BtcHotWallet } from '../wallet';

export default class TransferWalletTool<T extends BtcWallet | BtcHotWallet> extends TransactionTool<T> {
    constructor(wallet: T, hotMode?: boolean);
    private buildPsbt;
    transfer({ utxos, feeRate, recipient, changeAddress, transferUTXOs, }: TransferParams): import('..').TxDetail;
}
