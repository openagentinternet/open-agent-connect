import { TxDetail, TxCtxData } from '../types';
import { BtcWallet } from '../wallet';
import { UTXO } from '../../chain-base';
import { default as TaprootTool } from '../wallet-tools/TaprootTool';

export interface InscribeParams {
    utxos: UTXO[];
    feeRate: number;
    changeAddress?: string;
    revealOutValue?: number;
    /** @deprecated */
    service?: {
        address: string;
        satoshis: string;
    };
    outputs?: {
        address: string;
        satoshis: string;
    }[];
}
export declare class InscribeTool extends TaprootTool {
    txCtxDataList: TxCtxData[];
    constructor(wallet: BtcWallet);
    inscribe({ utxos, feeRate, service, outputs, changeAddress, revealOutValue, }: InscribeParams): {
        commitTx: TxDetail;
        revealTxs: TxDetail[];
    };
    private buildEmptyRevealPsbt;
    private buildCommitTx;
    private buildCommitPsbt;
    private completeRevealTx;
}
