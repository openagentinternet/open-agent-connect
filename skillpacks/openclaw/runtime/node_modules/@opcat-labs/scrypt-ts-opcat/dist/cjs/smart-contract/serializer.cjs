"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.deserializeOutputs = exports.serializeArgToHex = void 0;
const index_js_1 = require('./fns/index.cjs');
const tools = __importStar(require("uint8array-tools"));
const script_js_1 = require('./types/script.cjs');
const common_js_1 = require('../utils/common.cjs');
const bufferutils_js_1 = require('../psbt/bufferutils.cjs');
/**
 * @ignore
 * int to little-endian signed magnitude
 */
function int2hex(n) {
    if (n === BigInt(0)) {
        return '00';
    }
    else if (n === BigInt(-1)) {
        return '4f';
    }
    else if (n > BigInt(0) && n <= BigInt(16)) {
        n += BigInt(80);
        return n.toString(16);
    }
    return script_js_1.Script.fromASM((0, index_js_1.intToByteString)(n)).toHex();
}
/**
 * @ignore
 * @param b
 * @returns
 */
function bool2hex(b) {
    if (b) {
        return '51';
    }
    return '00';
}
/**
 * @ignore
 * @param b
 * @returns
 */
function bytes2hex(b) {
    if (b) {
        if (b.length / 2 > 1) {
            return script_js_1.Script.fromASM(b).toHex();
        }
        const intValue = parseInt(b, 16);
        if (intValue >= 1 && intValue <= 16) {
            return BigInt(intValue + 80).toString(16);
        }
        return script_js_1.Script.fromASM(b).toHex();
    }
    return '00';
}
/**
 * @ignore
 * @param x
 * @returns
 */
function serializeArgToHex(x) {
    if (typeof x === 'number' || typeof x === 'bigint') {
        return int2hex(x);
    }
    else if (typeof x === 'boolean') {
        return bool2hex(x);
    }
    else if (typeof x === 'string') {
        return bytes2hex(x);
    }
    throw new Error(`unsupport PrimitiveTypes: ${x}`);
}
exports.serializeArgToHex = serializeArgToHex;
/**
 * @ignore
 * @param outputsByte
 * @returns
 */
function deserializeOutputs(outputsByte) {
    const reader = new bufferutils_js_1.BufferReader(tools.fromHex(outputsByte));
    const outputs = [];
    try {
        while (reader.offset < reader.buffer.length) {
            const value = reader.readInt64();
            const scriptHash = (0, common_js_1.uint8ArrayToHex)(reader.readSlice(32));
            const dataHash = (0, common_js_1.uint8ArrayToHex)(reader.readSlice(32));
            outputs.push({ value, scriptHash, dataHash });
        }
    }
    catch (_error) {
        throw new Error(`Invalid format of serialized outputs: ${outputsByte}`);
    }
    return outputs;
}
exports.deserializeOutputs = deserializeOutputs;
//# sourceMappingURL=serializer.js.map