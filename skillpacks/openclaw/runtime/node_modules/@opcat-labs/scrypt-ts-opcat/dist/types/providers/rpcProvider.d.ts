import { ChainProvider } from './chainProvider.js';
import { UtxoProvider, UtxoQueryOptions } from './utxoProvider.js';
import { SupportedNetwork, TxId, UTXO } from '../globalTypes.js';
/**
 * The RPCProvider is backed by opcat RPC
 * @category Provider
 */
export declare class RPCProvider implements ChainProvider, UtxoProvider {
    readonly network: SupportedNetwork;
    readonly url: string;
    readonly walletName: string;
    readonly username: string;
    readonly password: string;
    private broadcastedTxs;
    private spentUTXOs;
    private newUTXOs;
    constructor(network: SupportedNetwork, url: string, walletName: string, username: string, password: string);
    getNetwork(): Promise<SupportedNetwork>;
    getFeeRate(): Promise<number>;
    protected getRpcUser: () => string;
    protected getRpcPassword: () => string;
    protected getRpcUrl: (walletName: string) => string;
    getConfirmations(txId: string): Promise<number>;
    private _broadcast;
    private _getConfirmations;
    broadcast(txHex: string): Promise<TxId>;
    broadcastPsbt(psbtBase64: string, metadata?: Record<string, unknown>): Promise<TxId>;
    getRawTransaction(txId: string): Promise<string>;
    private _getRawTransaction;
    getUtxos(address: string, _options?: UtxoQueryOptions): Promise<UTXO[]>;
    private isUnSpent;
    markSpent(txId: string, vout: number): void;
    addNewUTXO(utxo: UTXO): void;
    getMedianTime(): Promise<number>;
}
//# sourceMappingURL=rpcProvider.d.ts.map