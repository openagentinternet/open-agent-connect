"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.DEFAULT_BLOCK_EXPLORER_BASE_URL = exports.DEFAULT_MANAPI_BASE_URL = exports.DEFAULT_METASO_P2P_BASE_URL = void 0;
exports.createDefaultBrowserConfig = createDefaultBrowserConfig;
exports.resolveBrowserConfig = resolveBrowserConfig;
const botHomepageTemplates_js_1 = require("./botHomepageTemplates.js");
const metafileContentUrl_js_1 = require("./metafileContentUrl.js");
exports.DEFAULT_METASO_P2P_BASE_URL = 'https://so.metaid.io';
exports.DEFAULT_MANAPI_BASE_URL = 'https://manapi.metaid.io';
exports.DEFAULT_BLOCK_EXPLORER_BASE_URL = 'https://www.mvcscan.com/tx';
const DEFAULT_ENS_TEXT_KEY = 'org.openagentinternet.uri';
const DEFAULT_ENS_RPC_URLS = ['https://ethereum-rpc.publicnode.com'];
function normalizeUrl(value) {
    const text = typeof value === 'string' ? value.trim() : '';
    return text.replace(/\/+$/, '');
}
function normalizeBoolean(value) {
    if (typeof value === 'boolean')
        return value;
    if (typeof value !== 'string')
        return null;
    const normalized = value.trim().toLowerCase();
    if (normalized === 'true' || normalized === '1' || normalized === 'yes')
        return true;
    if (normalized === 'false' || normalized === '0' || normalized === 'no')
        return false;
    return null;
}
function normalizeUrlList(value) {
    const text = typeof value === 'string' ? value : Array.isArray(value) ? value.join(',') : '';
    return text
        .split(',')
        .map((item) => normalizeUrl(item))
        .filter(Boolean);
}
function normalizeText(value) {
    return typeof value === 'string' ? value.trim() : '';
}
function createDefaultBrowserConfig() {
    return {
        metasoP2PBaseUrl: exports.DEFAULT_METASO_P2P_BASE_URL,
        metafileContentBaseUrl: metafileContentUrl_js_1.DEFAULT_METAFILE_CONTENT_BASE_URL,
        manApiBaseUrl: exports.DEFAULT_MANAPI_BASE_URL,
        blockExplorerBaseUrl: exports.DEFAULT_BLOCK_EXPLORER_BASE_URL,
        walletApiBaseUrl: '',
        botHomepageTemplateId: botHomepageTemplates_js_1.DEFAULT_BOT_HOMEPAGE_TEMPLATE_ID,
        renderCustomBotPages: true,
        nameResolution: {
            enabled: true,
            ens: {
                enabled: true,
                chainId: 1,
                rpcUrls: [...DEFAULT_ENS_RPC_URLS],
                textKey: DEFAULT_ENS_TEXT_KEY,
            },
        },
        localMode: false,
    };
}
function resolveBrowserConfig(config = {}, env = {}) {
    const defaults = createDefaultBrowserConfig();
    const browser = config.browser ?? {};
    const browserNameResolution = (browser.nameResolution ?? {});
    const browserEns = (browserNameResolution.ens ?? {});
    const envRpcUrls = normalizeUrlList(env.METABOT_BROWSER_ENS_RPC_URLS);
    const browserRpcUrls = normalizeUrlList(browserEns.rpcUrls);
    const configuredRpcUrls = envRpcUrls.length > 0
        ? envRpcUrls
        : browserRpcUrls.length > 0
            ? browserRpcUrls
            : [...defaults.nameResolution.ens.rpcUrls];
    const nameResolutionEnabled = normalizeBoolean(env.METABOT_BROWSER_NAME_RESOLUTION_ENABLED)
        ?? (typeof browserNameResolution.enabled === 'boolean' ? browserNameResolution.enabled : defaults.nameResolution.enabled);
    const ensEnabledInput = normalizeBoolean(env.METABOT_BROWSER_ENS_ENABLED)
        ?? (typeof browserEns.enabled === 'boolean' ? browserEns.enabled : configuredRpcUrls.length > 0);
    const ensTextKey = normalizeText(env.METABOT_BROWSER_ENS_TEXT_KEY)
        || normalizeText(browserEns.textKey)
        || defaults.nameResolution.ens.textKey;
    return {
        metasoP2PBaseUrl: normalizeUrl(env.METABOT_BROWSER_METASO_P2P_BASE_URL)
            || normalizeUrl(browser.metasoP2PBaseUrl)
            || defaults.metasoP2PBaseUrl,
        metafileContentBaseUrl: normalizeUrl(env.METABOT_BROWSER_METAFILE_CONTENT_BASE_URL)
            || normalizeUrl(browser.metafileContentBaseUrl)
            || defaults.metafileContentBaseUrl,
        manApiBaseUrl: normalizeUrl(env.METABOT_BROWSER_MANAPI_BASE_URL)
            || normalizeUrl(browser.manApiBaseUrl)
            || defaults.manApiBaseUrl,
        blockExplorerBaseUrl: normalizeUrl(env.METABOT_BROWSER_BLOCK_EXPLORER_BASE_URL)
            || normalizeUrl(browser.blockExplorerBaseUrl)
            || defaults.blockExplorerBaseUrl,
        walletApiBaseUrl: normalizeUrl(env.METABOT_BROWSER_WALLET_API_BASE_URL)
            || normalizeUrl(browser.walletApiBaseUrl)
            || defaults.walletApiBaseUrl,
        botHomepageTemplateId: (0, botHomepageTemplates_js_1.normalizeBotHomepageTemplateId)(browser.botHomepageTemplateId),
        renderCustomBotPages: typeof browser.renderCustomBotPages === 'boolean'
            ? browser.renderCustomBotPages
            : defaults.renderCustomBotPages,
        nameResolution: {
            enabled: nameResolutionEnabled,
            ens: {
                enabled: nameResolutionEnabled && ensEnabledInput && configuredRpcUrls.length > 0,
                chainId: 1,
                rpcUrls: configuredRpcUrls,
                textKey: ensTextKey,
            },
        },
        localMode: typeof browser.localMode === 'boolean' ? browser.localMode : defaults.localMode,
    };
}
//# sourceMappingURL=config.js.map