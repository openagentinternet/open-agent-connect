import { type MetaAppLaunchContext } from '../browser/metaAppLaunchContext.js';
export type BrowserUriScheme = 'metaid' | 'metaapp' | 'metafile' | 'map' | 'pin' | 'preview-metaapp';
export interface ParsedBrowserUri {
    originalUri: string;
    normalizedUri: string;
    scheme: BrowserUriScheme;
    id: string;
    /** Present when the scheme is metaapp and the deep link carries launch parameters. */
    launchContext?: MetaAppLaunchContext;
}
export declare function isValidGlobalMetaId(input: string): boolean;
export declare function parseBrowserUri(input: string): ParsedBrowserUri;
//# sourceMappingURL=browserUri.d.ts.map