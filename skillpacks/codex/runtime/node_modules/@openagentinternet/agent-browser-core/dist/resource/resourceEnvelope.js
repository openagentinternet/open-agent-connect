export function normalizeResourceSections(value) {
    if (!Array.isArray(value))
        return [];
    return value.flatMap((section) => {
        if (!section || typeof section !== 'object')
            return [];
        const raw = section;
        const id = typeof raw.id === 'string' && raw.id.trim() ? raw.id.trim() : '';
        const title = typeof raw.title === 'string' && raw.title.trim() ? raw.title.trim() : '';
        const kind = typeof raw.kind === 'string' && raw.kind.trim() ? raw.kind.trim() : 'generic-list';
        const items = Array.isArray(raw.items)
            ? raw.items.filter((item) => Boolean(item) && typeof item === 'object' && !Array.isArray(item))
            : [];
        if (!id || !title)
            return [];
        return [{
                id,
                title,
                kind: isResourceSectionKind(kind) ? kind : 'generic-list',
                items,
            }];
    });
}
export function normalizeTrustedActions(value) {
    if (!Array.isArray(value))
        return [];
    return value.flatMap((action) => {
        if (!action || typeof action !== 'object')
            return [];
        const raw = action;
        const id = typeof raw.id === 'string' && raw.id.trim() ? raw.id.trim() : '';
        const label = typeof raw.label === 'string' && raw.label.trim() ? raw.label.trim() : '';
        const kind = typeof raw.kind === 'string' && raw.kind.trim() ? raw.kind.trim() : '';
        if (!id || !label || !isResolveActionKind(kind))
            return [];
        return [{
                id,
                label,
                kind,
                enabled: raw.enabled !== false,
                ...(raw.payload && typeof raw.payload === 'object' && !Array.isArray(raw.payload) ? { payload: raw.payload } : {}),
            }];
    });
}
export function createUnsupportedResourceEnvelope(uri, message) {
    return {
        uri,
        normalizedUri: uri,
        resourceType: 'unknown',
        title: 'Unsupported resource',
        owner: {
            kind: 'unknown',
            name: 'Unknown owner',
            verificationState: 'unverified',
        },
        renderer: {
            type: 'unsupported',
            contentType: 'text/plain',
            error: message,
        },
        actions: [],
        sections: [],
        status: {
            state: 'error',
            verificationState: 'unverified',
            message,
        },
        source: {
            resolver: 'unsupported-resource',
        },
    };
}
function isResourceSectionKind(value) {
    return ['services', 'skills', 'buses', 'buzzes', 'apps', 'activity', 'generic-list'].includes(value);
}
function isResolveActionKind(value) {
    return [
        'private-chat',
        'service-list',
        'service-call',
        'copy',
        'proof',
        'creator',
    ].includes(value);
}
//# sourceMappingURL=resourceEnvelope.js.map