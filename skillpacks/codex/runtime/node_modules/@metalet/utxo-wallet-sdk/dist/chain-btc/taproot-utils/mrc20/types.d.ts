import { Chain } from '../../../index';
import { UTXO } from '../../../chain-base';

export interface MRC20UTXO extends UTXO {
    chain: Chain;
    address: string;
    scriptPk: string;
    timestamp: number;
    mrc20s: MRC20Info[];
    blockHeight: number;
}
interface MRC20Info {
    tick: string;
    mrc20Id: string;
    txPoint: string;
    amount: string;
    balance: string;
    decimals: string;
    tokenName: string;
}
export {};
