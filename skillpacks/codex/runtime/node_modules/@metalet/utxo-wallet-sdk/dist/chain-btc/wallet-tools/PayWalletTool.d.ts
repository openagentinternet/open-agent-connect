import { PayParams } from '../types';
import { default as TransactionTool } from './TransactionTool';
import { BtcWallet, BtcHotWallet } from '../wallet';

export default class PayWalletTool<T extends BtcWallet | BtcHotWallet> extends TransactionTool<T> {
    constructor(wallet: T, hotMode?: boolean);
    private buildPsbt;
    pay({ utxos, feeRate, changeAddress, psbtHex, autoFinalized }: PayParams): import('..').TxDetail;
}
