"use strict";
var __decorate = (this && this.__decorate) || function (decorators, target, key, desc) {
    var c = arguments.length, r = c < 3 ? target : desc === null ? desc = Object.getOwnPropertyDescriptor(target, key) : desc, d;
    if (typeof Reflect === "object" && typeof Reflect.decorate === "function") r = Reflect.decorate(decorators, target, key, desc);
    else for (var i = decorators.length - 1; i >= 0; i--) if (d = decorators[i]) r = (c < 3 ? d(r) : c > 3 ? d(target, key, r) : d(target, key)) || r;
    return c > 3 && r && Object.defineProperty(target, key, r), r;
};
var __metadata = (this && this.__metadata) || function (k, v) {
    if (typeof Reflect === "object" && typeof Reflect.metadata === "function") return Reflect.metadata(k, v);
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.StdUtils = exports.UINT32_MIN = exports.UINT32_MAX = exports.UINT64_MIN = exports.UINT64_MAX = void 0;
const decorators_js_1 = require('../decorators.cjs');
const assert_js_1 = require('../fns/assert.cjs');
const byteString_js_1 = require('../fns/byteString.cjs');
const smartContractLib_js_1 = require('../smartContractLib.cjs');
exports.UINT64_MAX = 0xffffffffffffffffn;
exports.UINT64_MIN = 0n;
exports.UINT32_MAX = 0xffffffffn;
exports.UINT32_MIN = 0n;
/**
 * A utility class providing standard helper methods for working with ByteStrings and numeric conversions.
 * Includes methods for:
 * - Checking ByteString length divisibility
 * - Converting between numeric types (UInt32, UInt64) and ByteStrings
 * - Little-endian unsigned integer conversions
 * - Variable-length integer (VarInt) serialization/deserialization
 * - Push data serialization for Bitcoin script
 * All methods are static and annotated with @method() decorator.
 */
class StdUtils extends smartContractLib_js_1.SmartContractLib {
    /**
     * Checks if the length of a ByteString is divisible by a given number and returns the quotient.
     * @param b The ByteString to check
     * @param n The divisor to check against
     * @returns The quotient of the division (length / n)
     * @throws If the length is not divisible by n
     */
    static checkLenDivisibleBy(b, n) {
        const l = (0, byteString_js_1.len)(b);
        (0, assert_js_1.assert)(l % n == 0n, 'length of b is not divisible by n');
        return l / n;
    }
    /**
     * Converts a UInt64 value to a little-endian ByteString.
     * @param n The UInt64 value to convert
     * @returns The little-endian ByteString representation
     * @throws If the input value is outside UInt64 range (0 to 2^64-1)
     */
    static uint64ToByteString(n) {
        (0, assert_js_1.assert)(n >= exports.UINT64_MIN && n <= exports.UINT64_MAX, 'uint64 out of range');
        return StdUtils.toLEUnsigned(n, 8n);
    }
    /**
     * Converts a UInt32 number to a 4-byte little-endian ByteString.
     * @param n The UInt32 number to convert
     * @returns The resulting 4-byte little-endian ByteString
     * @throws If the input number is outside the UInt32 range (0 to 4294967295)
     */
    static uint32ToByteString(n) {
        (0, assert_js_1.assert)(n >= exports.UINT32_MIN && n <= exports.UINT32_MAX, 'uint32 out of range');
        return StdUtils.toLEUnsigned(n, 4n);
    }
    /**
     * Converts a 4-byte ByteString to an unsigned 32-bit integer in little-endian format.
     * @param b - The ByteString to convert (must be exactly 4 bytes long)
     * @returns The converted UInt32 value
     * @throws Will throw an error if the input ByteString length is not 4 bytes
     */
    static byteStringToUInt32(b) {
        (0, assert_js_1.assert)((0, byteString_js_1.len)(b) == 4n, 'byteStringToUInt32: byteString length is not 4');
        return StdUtils.fromLEUnsigned(b);
    }
    /**
     * convert signed integer `n` to unsigned integer of `l` string, in little endian
     * @param {bigint} n the number to be converted
     * @param {bigint} l expected length
     * @returns {ByteString} returns a `ByteString`
     */
    static toLEUnsigned(n, l) {
        const m = (0, byteString_js_1.intToByteString)(n, l + 1n);
        // remove sign byte
        return (0, byteString_js_1.slice)(m, 0n, l);
    }
    /**
     * convert `ByteString` to unsigned integer, in sign-magnitude little endian
     * @param {ByteString} bytes the `ByteString` to be converted
     * @returns {bigint} returns a number
     */
    static fromLEUnsigned(b) {
        return (0, byteString_js_1.byteStringToInt)(b + (0, byteString_js_1.toByteString)('00'));
    }
    /**
     * Encodes a bigint into a variable-length integer (VarInt) format as ByteString.
     * The encoding follows the standard VarInt scheme:
     * - Values < 0xfd: encoded as 1 byte
     * - Values < 0x10000: prefixed with 0xfd and encoded as 2 bytes (little-endian)
     * - Values < 0x100000000: prefixed with 0xfe and encoded as 4 bytes (little-endian)
     * - Larger values: prefixed with 0xff and encoded as 8 bytes (little-endian)
     * @param n - The bigint value to encode
     * @returns ByteString containing the VarInt encoded value
     */
    static writeVarInt(n) {
        let b = (0, byteString_js_1.toByteString)('');
        let size = 0n;
        if (n < 0xfdn) {
            size = 1n;
        }
        else if (n < 0x10000n) {
            b = StdUtils.VARINT_2BYTE;
            size = 2n;
        }
        else if (n < 0x100000000n) {
            b = StdUtils.VARINT_4BYTE;
            size = 4n;
        }
        else {
            b = StdUtils.VARINT_8BYTE;
            size = 8n;
        }
        return b + StdUtils.toLEUnsigned(n, size);
    }
    /**
     * Pushes data to a buffer with appropriate size header.
     *
     * @param buf - The input data as a ByteString
     * @returns The input data prefixed with appropriate size header in little-endian format
     * @remarks The header format follows standard Bitcoin script pushdata rules:
     *          - 0x4c for 1-byte length (0-255 bytes)
     *          - 0x4d for 2-byte length (256-65535 bytes)
     *          - 0x4e for 4-byte length (65536-4294967295 bytes)
     * @throws Will assert if input size exceeds 32-bit limit (4294967295 bytes)
     */
    static pushData(buf) {
        const n = (0, byteString_js_1.len)(buf);
        let size = 0n;
        let header = (0, byteString_js_1.toByteString)('');
        if (n < StdUtils.OP_PUSHDATA1_VAL) {
            size = 1n;
            header = (0, byteString_js_1.toByteString)('');
        }
        else if (n < 0x100) {
            size = 1n;
            header = (0, byteString_js_1.toByteString)('4c');
        }
        else if (n < 0x10000) {
            size = 2n;
            header = (0, byteString_js_1.toByteString)('4d');
        }
        else if (n < 0x100000000) {
            size = 4n;
            header = (0, byteString_js_1.toByteString)('4e');
        }
        else {
            // shall not reach here
            (0, assert_js_1.assert)(false);
        }
        return header + StdUtils.toLEUnsigned(n, size);
    }
    /**
       * read [VarInt (variable integer)]{@link https://learnmeabitcoin.com/technical/general/compact-size/}-encoded data from the beginning of 'buf'
       * @param {ByteString} buf a buffer `ByteString` of format: [prefix FD/FE/FF +] length + data
       * @returns return data
       */
    static readVarint(buf, pos) {
        let l = 0n;
        let ret = (0, byteString_js_1.toByteString)('');
        let nextPos = pos;
        const header = (0, byteString_js_1.slice)(buf, pos, pos + 1n);
        if (header == StdUtils.VARINT_2BYTE) {
            l = StdUtils.fromLEUnsigned((0, byteString_js_1.slice)(buf, pos + 1n, pos + 3n));
            ret = (0, byteString_js_1.slice)(buf, pos + 3n, pos + 3n + l);
            nextPos = pos + 3n + l;
        }
        else if (header == StdUtils.VARINT_4BYTE) {
            l = StdUtils.fromLEUnsigned((0, byteString_js_1.slice)(buf, pos + 1n, pos + 5n));
            ret = (0, byteString_js_1.slice)(buf, pos + 5n, pos + 5n + l);
            nextPos = pos + 5n + l;
        }
        else if (header == StdUtils.VARINT_8BYTE) {
            l = StdUtils.fromLEUnsigned((0, byteString_js_1.slice)(buf, pos + 1n, pos + 9n));
            ret = (0, byteString_js_1.slice)(buf, pos + 9n, pos + 9n + l);
            nextPos = pos + 9n + l;
        }
        else {
            l = StdUtils.fromLEUnsigned((0, byteString_js_1.slice)(buf, pos, pos + 1n));
            ret = (0, byteString_js_1.slice)(buf, pos + 1n, pos + 1n + l);
            nextPos = pos + 1n + l;
        }
        return { data: ret, nextPos: nextPos };
    }
}
exports.StdUtils = StdUtils;
StdUtils.VARINT_2BYTE = (0, byteString_js_1.toByteString)('fd');
StdUtils.VARINT_4BYTE = (0, byteString_js_1.toByteString)('fe');
StdUtils.VARINT_8BYTE = (0, byteString_js_1.toByteString)('ff');
StdUtils.OP_PUSHDATA1_VAL = 0x4cn;
StdUtils.OP_PUSHDATA2_VAL = 0x4dn;
StdUtils.OP_PUSHDATA4_VAL = 0x4en;
__decorate([
    (0, decorators_js_1.prop)(),
    __metadata("design:type", String)
], StdUtils, "VARINT_2BYTE", void 0);
__decorate([
    (0, decorators_js_1.prop)(),
    __metadata("design:type", String)
], StdUtils, "VARINT_4BYTE", void 0);
__decorate([
    (0, decorators_js_1.prop)(),
    __metadata("design:type", String)
], StdUtils, "VARINT_8BYTE", void 0);
__decorate([
    (0, decorators_js_1.prop)(),
    __metadata("design:type", BigInt)
], StdUtils, "OP_PUSHDATA1_VAL", void 0);
__decorate([
    (0, decorators_js_1.prop)(),
    __metadata("design:type", BigInt)
], StdUtils, "OP_PUSHDATA2_VAL", void 0);
__decorate([
    (0, decorators_js_1.prop)(),
    __metadata("design:type", BigInt)
], StdUtils, "OP_PUSHDATA4_VAL", void 0);
__decorate([
    (0, decorators_js_1.method)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, BigInt]),
    __metadata("design:returntype", BigInt)
], StdUtils, "checkLenDivisibleBy", null);
__decorate([
    (0, decorators_js_1.method)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [BigInt]),
    __metadata("design:returntype", String)
], StdUtils, "uint64ToByteString", null);
__decorate([
    (0, decorators_js_1.method)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [BigInt]),
    __metadata("design:returntype", String)
], StdUtils, "uint32ToByteString", null);
__decorate([
    (0, decorators_js_1.method)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", BigInt)
], StdUtils, "byteStringToUInt32", null);
__decorate([
    (0, decorators_js_1.method)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [BigInt, BigInt]),
    __metadata("design:returntype", String)
], StdUtils, "toLEUnsigned", null);
__decorate([
    (0, decorators_js_1.method)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", BigInt)
], StdUtils, "fromLEUnsigned", null);
__decorate([
    (0, decorators_js_1.method)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [BigInt]),
    __metadata("design:returntype", String)
], StdUtils, "writeVarInt", null);
__decorate([
    (0, decorators_js_1.method)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String]),
    __metadata("design:returntype", String)
], StdUtils, "pushData", null);
__decorate([
    (0, decorators_js_1.method)(),
    __metadata("design:type", Function),
    __metadata("design:paramtypes", [String, BigInt]),
    __metadata("design:returntype", Object)
], StdUtils, "readVarint", null);
//# sourceMappingURL=stdUtils.js.map